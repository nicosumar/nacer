- content_for :title do
  Modificación de la solicitud

#content-header
  %h1 Modificación de la solicitud

= render :partial => "shared/info", :locals => { :registro => @novedad }

#content-body
  %h2.first-header Información de la solicitud que se modificará

  = render "form"
