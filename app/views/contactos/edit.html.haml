- content_for :title do
  Modificación del contacto

#content-header
  %h1 Modificación del contacto

= render :partial => "shared/info", :locals => { :registro => @contacto }

#content-body
  %h2.first-header Información del contacto que se modificará

  = render 'form'
