.infobox-ok=(notice)

%h1 Contactos

%ul{:id => "submenu"}
  - if can? :create, Contacto
    %li= link_to "Agregar un contacto", new_contacto_url, :class => "drop"



%p

%table
  %thead
    %tr
      %th Nombre
      %th E-mail
      %th Teléfonos
  %tbody
    - @contactos.each do |c|
      %tr.clickable{ :onclick => "location.href=\'#{contacto_path(c)}\'", :title => "Ver detalles del contacto \"#{c.mostrado}\"" }
        %td= (c.apellidos ? c.apellidos + (c.nombres ? ", " + c.nombres : "") : c.mostrado)
        %td= c.email
        %td= (c.telefono || "") + (c.telefono_movil || "")
= will_paginate @contactos, :inner_window => 2, :outer_window => 0


