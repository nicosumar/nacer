- content_for :title do
  Modificación de datos carga masiva

#content-header
  %h1 Modificación de datos de la carga masiva

= render :partial => "/shared/info", :locals => { :registro => @carga_masiva }

#content-body
  %h2.first-header-with-note Formulario de modificación de datos de la carga masiva
  %p.note Los campos marcados con asterisco (*) son obligatorios

  = render 'form'
