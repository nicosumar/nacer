- content_for :title do
  Creación de una nueva liquidación

#content-header
  %h1 Creación de una nueva liquidación

= render :partial => "shared/info", :locals => { :registro => @liquidacion }

#content-body
  %h2.first-header-with-note Formulario de creación de una liquidación
  %p.note Los campos marcados con asterisco (*) son obligatorios

  = render 'form'
