- content_for :title do
  Iniciar la sesión

#content-body
  %h1.first-header Iniciar la sesión

  = render :partial => "shared/info", :locals => { :registro => @user_session }

  %h2 Ingrese sus credenciales de acceso

  = render :partial => 'user_sessions/form'
