- content_for :title do
  = @informe.titulo

#content-header
  %h1= @informe.titulo

#content-body
  %h2.first-header= @informe.titulo
  = render(:partial => @informe.nombre_partial, :locals => {:i => @informes})