<%= form_for(@documento_generable) do |f| %>
  <% if @documento_generable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@documento_generable.errors.count, "error") %> prohibited this documento_generable from being saved:</h2>

      <ul>
      <% @documento_generable.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :modelo %><br />
    <%= f.text_field :modelo %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
