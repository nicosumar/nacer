- content_for :title do
  Modificación de la adenda

#content-header
  %h1 Modificación de la adenda

= render :partial => "/shared/info", :locals => { :registro => @addenda }

#content-body
  %h2.first-header Información de la adenda que se modificará

  = render 'form'
