- content_for :title do
  Creación de una nueva adenda

#content-header
  %h1 Creación de una nueva adenda

= render :partial => "shared/info", :locals => { :registro => @addenda }

#content-body
  %h2.first-header Información de la adenda que se creará

  = render 'form'
