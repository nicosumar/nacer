#content-header
  %h1 Editar informe de debito
  #action-links
    = link_to '<< Volver al listado', informes_debitos_prestacionales_path
    = link_to 'Ver', @informe_debito_prestacional

#content-body
  = render :partial => "shared/info", :locals => { :registro => @informe_debito_prestacional }
  = render 'form'